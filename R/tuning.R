# Tuning parameter selection (pseudo-BIC, grid search, etc.)

pseudo_bic <- function(theta_hat, S, n) {
  stop("TODO")
}

select_lambda_by_bic <- function(fit_fn, S, n, lambda_grid) {
  stop("TODO")
}
